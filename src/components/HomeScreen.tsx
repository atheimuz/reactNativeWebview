import React from 'react';
import {StyleSheet, Text} from 'react-native';

const HomeScreen = () => <Text>홈</Text>;

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
});

export default HomeScreen;
